import {Algorithm, Default} from './core/default'
import {HOTP} from './core/hotp';
import {TOTP} from './core/totp';

const notp = {
    Algorithm,
    Default,
    HOTP,
    TOTP
};

export default notp;
